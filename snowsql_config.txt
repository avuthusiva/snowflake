https://signup.snowflake.com/?trial=student

snowsql -a PQJQVNA-NQC12425 -u asvr410

for installing snowsql on ubuntu
first need to go to snowflake snowsql page and download the bash file
provide the executable permission
write the command as 

sudo bash filename
/opt/bin --need to provide the file path


then need to configure the path variable
open the file ~/.bashrc

export PATH="$PATH:/opt/bin"
source ~/.bashrc
snowsql --upgrade 
cd ~/.snowsql/
code config
snowsql -c username

now enter the command as snowsql -v
